import 'package:flutter/material.dart';

class FeriadosPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text('Página de feriados'),
        ),
      ),
    );
  }
}